Quick crypto check for LANAgent. SSH: see CLAUDE.local.md for credentials. Diag script: `./scripts/diag-crypto.sh health` (full check), `status` (API state), `trades 50`, `errors 20`, `pnl 30`, `grep "pattern" 30 crypto.log`. API base: `http://$PRODUCTION_SERVER/api/crypto` with header `x-api-key: your-api-key`. Key endpoints: `GET /strategy/status`, `GET /strategy/token-trader/status`, `POST /strategy/trigger`. Two strategies run concurrently: dollar_maximizer (BNB swing trading, holding stablecoins ~$315 USDT) and token_trader (currently SIREN on BSC, config v5). Token trader is regime-based (ENTERING→SIDEWAYS→DIP/PUMP/MOON/DUMP→COOLDOWN) with scale-out levels [10,20,30,40,50]% selling {10:10%,20:15%,30:20%,40:20%,50:35%} of position, trailing stops at 4.5% (PUMP/MOON) / 5% (MOON>50%) / 8% (standard), pump stall detection (10% sell after 6hr without next level), DIP trend gates (short<-0.5, long<-10%), -15% stop-loss, -8%/hr emergency 50% sell, and circuit breaker (3 stops or -$150 cumulative). CRITICAL: forceV3=true always for token_trader swaps, expectedOutputUsd always set with 70% sanity threshold, never allow V2 fallback. Deploy: `node --check` then `./scripts/deployment/deploy-files.sh src/path/file.js`, restart with `pm2 restart ecosystem.config.cjs`. Full reference: `docs/claude/CLAUDE_CRYPTO_DIAG.md`.

node_modules/
archive/
.env
logs/
data/
*.log
.DS_Store
.vscode/
.idea/
coverage/
dist/
build/
*.swp
*.swo
*~
.npm
.cache/
uploads/
quarantine/
workspace/
.claude/
test-*.js
*.backup
*.tmp
data/ssh_host_key
notes_not_for_repo.txt

# Instance-specific files (generated by install script)
CLAUDE.local.md
scripts/deployment/deploy.config

# Sensitive files
docs/api/API_README.md.bak
*.bak
*.bak
scripts/work.sh
scripts/deployment/deploy.local

# Session reports are genesis-only — never publish (privacy)
docs/sessions/

# Genesis-only private content — defense-in-depth so a stray copy can't be
# accidentally tracked in public. Enforced at push time by .git/hooks/pre-push
# (see scripts/privacy-scan.sh + scripts/install-git-hooks.sh).
src/services/crypto/strategies/ArbitrageStrategy.js
src/services/crypto/strategies/DollarMaximizerStrategy.js
src/services/crypto/strategies/NativeMaximizerStrategy.js
src/services/crypto/strategies/RuleBasedStrategy.js
src/services/crypto/strategies/TokenTraderStrategy.js
src/services/crypto/strategies/VolatilityAdjustedStrategy.js
# VRM / 3D model binaries (kept private)
*.vrm
*.glb
*.gltf
*.fbx
# Trained ML models (instance-specific, e.g. wake-word) — genesis-only
*.onnx

# Operator-side privacy/sync tooling — keep on disk (pre-push hook needs it),
# never publish (contains infra-detection patterns).
scripts/privacy-scan.sh
scripts/install-git-hooks.sh
scripts/genesis-to-public-sync.sh
scripts/git-hooks/
